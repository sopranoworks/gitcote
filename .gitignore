# Compiled binary (built via `go build -o gitcote ./cmd/gitcote`)
/gitcote

# Go test binaries
*.test

# Local runtime config and data
/gitcote.yaml
/data/

# Frontend
web/node_modules/
cmd/gitcote/dist/
